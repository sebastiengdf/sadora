{% if breadcrumb %}
    {% for item in breadcrumb %}
        {% if item.url %}
          	<a href="{{ item.url }}">{{ item.text }}</a> <i> / </i>
        {% else %}
          	{{ item.text }}
        {% endif %}
    {% endfor %}
{% endif %}