{% block content %}
	<div{{ attributes }}>
        {{ title_prefix }}
            {% if label %}
                <h2 {{ title_attributes }}>{{ label }}</h2>
            {% endif %}
        {{ title_suffix }}
        <nav>
	        {{ content }}
	    </nav>
    </div>
{% endblock %}